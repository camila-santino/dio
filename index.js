const nome = "Nonsense";
let qtdXp = 1;

switch (qtdXp) {
    case qtdXp <= 1000:
    case qtdXp >= 1:
        console.log("O Herói " + nome + " está no nível Ferro");
        break;
        const nome = "Nonsense"      
    case qtdXp <= 1000:
    console.log("O Herói " + nome + " está no nível zerado");
    break;
}