const nome = "Nonsense";
let qtdXp = 1;

switch (qtdXp) {
    case qtdXp <= 1000:
    case qtdXp >= 1:
        console.log("O Herói " + nome + " está no nível Ferro");
        break;
        const nome = "Nonsense"      
    case qtdXp <= 1000:
    console.log("O Herói " + nome + " está no nível zerado");
    break;
    const nome = "Nonsense";
    let qtdXp = 1;
    
    switch (qtdXp) {
        case (qtdXp <= 1000):
        case (qtdXp >= 1):
            console.log("O Herói " + nome + " está no nível Ferro");
        break;
        case qtdXp <= 1000:
        case qtdXp >= 1:
            console.log("O Herói " + nome + " está no nível Ferro");
        break;
        case qtdXp <= 1000:
        case qtdXp >= 1:
            console.log("O Herói " + nome + " está no nível Ferro");
        break;
        case qtdXp <= 1000:
        case qtdXp >= 1:
            console.log("O Herói " + nome + " está no nível Ferro");
        break;
        case qtdXp <= 1000:
        case qtdXp >= 1:
            console.log("O Herói " + nome + " está no nível Ferro");
        break;
        case default:
           console.log("O Herói não iniciou a quest.");
        break;
    }